# Docker Configuration
# Docker image tag for building and running the container
IMAGE_TAG=latest

# gRPC Server Configuration
# Port on which the gRPC server will listen
GRPC_PORT=50051

# Database Configuration
# Path to SQLite database file (relative to backend directory)
DB_PATH=database.db
